import 'package:flutter/material.dart';

class Exibitions extends StatelessWidget {
  const Exibitions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Icon(Icons.high_quality),
      ),
    );
  }
}
